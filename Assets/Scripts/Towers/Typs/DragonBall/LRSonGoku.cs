using UnityEngine;

public class LRSonGoku : TowerBase
{
    public override void SpezialAngriff()
    {
        // Hier könnte die spezifische Logik für den Spezialangriff von LRSonGoku implementiert werden.
        // Zum Beispiel könnte dies ein mächtiger Angriff sein, der mehrere Gegner in einem mittleren Bereich trifft oder zusätzlichen Schaden verursacht.
        Debug.Log("LRSonGoku führt seinen Spezialangriff aus!");
    }
}
