using UnityEngine;

public class URSonGoku : TowerBase
{
    public override void SpezialAngriff()
    {
        // Hier könnte die spezifische Logik für den Spezialangriff von URSonGoku implementiert werden.
        // Zum Beispiel könnte dies ein extrem mächtiger Angriff sein, der alle Gegner in einem großen Bereich trifft oder erheblichen Schaden verursacht.
        Debug.Log("URSonGoku führt seinen Spezialangriff aus!");
    }
}
