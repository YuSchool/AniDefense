using UnityEngine;

public class LRZorro : TowerBase
{
    public override void SpezialAngriff()
    {
        // Hier könnte die spezifische Logik für den Spezialangriff von LRZorro implementiert werden.
        // Zum Beispiel könnte dies ein schneller Angriff sein, der mehrere Gegner in einem kleinen Bereich trifft oder zusätzlichen Schaden verursacht.
        Debug.Log("LRZorro führt seinen Spezialangriff aus!");
    }
}
