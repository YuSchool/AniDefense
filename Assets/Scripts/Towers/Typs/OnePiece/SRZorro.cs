using UnityEngine;

public class SRZorro : TowerBase
{
    public override void SpezialAngriff()
    {
        // Hier könnte die spezifische Logik für den Spezialangriff von SRZorro implementiert werden.
        // Zum Beispiel könnte dies ein schneller Angriff sein, der mehrere Gegner in einem kleinen Bereich trifft oder zusätzlichen Schaden verursacht.
        Debug.Log("SRZorro führt seinen Spezialangriff aus!");
    }
}
